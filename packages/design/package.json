{
  "name": "@nonet/design",
  "version": "0.0.0",
  "private": true,
  "description": "Nonet design tokens — the single source of truth for styling. Transcribed from the Claude Design prototype's Tokens screen.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
