// Placeholder. Design tokens (colour, type, space, motion, border, shadow) are
// transcribed from the prototype's Tokens screen in Phase 2 — see docs/ROADMAP.md.
//
// One token question is unresolved and must be settled by a human before tokens
// are baked in: `--fg3` measures 4.31:1 against `--bg` in light mode (below WCAG
// AA) and it carries every caption in the product. See design/README.md.
export {};
