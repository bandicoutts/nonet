# @nonet/web

Stub. The Next.js 16 app (App Router, SSR, Tailwind wired to `@nonet/design`) is
scaffolded in **Phase 3** — see [`docs/ROADMAP.md`](../../docs/ROADMAP.md).

Routes when built: `/` Home (daily hero, streak band, practice picker),
`/board`, `/solved`, `/archive`, `/record`, `/settings`, `/how-to-play`,
`/about`, `/auth`. There is no `/practice` route.
